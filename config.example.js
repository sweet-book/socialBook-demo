/**
 * 설정 파일 — config.js로 복사한 후 값을 채우세요.
 */
const APP_CONFIG = {
    environments: {
        live: { label: '운영', url: 'https://api.sweetbook.com/v1', apiKey: '' },
        sandbox: { label: '샌드박스', url: 'https://api-sandbox.sweetbook.com/v1', apiKey: '' },
    },
    defaultEnv: 'sandbox',
    useCookie: false,
    googleClientId: '',
    googleApiKey: '',
};
